{
  "name": "claude-code-session",
  "version": "0.1.0",
  "description": "Interactive Claude Code session picker",
  "bin": {
    "claude-code-session": "./claude-sessions.js",
    "codex-code-session": "./codex-sessions.js",
    "opencode-code-session": "./opencode-sessions.js"
  },
  "scripts": {
    "test": "node --test claude-sessions.test.js codex-sessions.test.js opencode-sessions.test.js install.test.js install-windows.test.js",
    "check": "node --check claude-sessions.js && node --check codex-sessions.js && node --check opencode-sessions.js && node --check session-utils.js && bash -n install.sh"
  },
  "keywords": [
    "claude",
    "claude-code",
    "session",
    "cli"
  ],
  "author": "lizhian",
  "license": "MIT"
}
